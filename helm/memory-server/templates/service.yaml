apiVersion: v1
kind: Service
metadata:
  name: {{ include "memory-server.fullname" . }}
  labels:
    {{- include "memory-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "memory-server.selectorLabels" . | nindent 4 }}
